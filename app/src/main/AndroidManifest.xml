<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.carpoolapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CarPoolApp">
        <activity
            android:name=".Controller.SignUpActivity"
            android:exported="false" />
        <activity
            android:name=".Controller.VehicleProfileActivity"
            android:exported="false" />
        <activity
            android:name=".Controller.AddVehicleActivity"
            android:exported="false" />
        <activity
            android:name=".Controller.VehiclesInfoActivity"
            android:exported="false" />
        <activity
            android:name=".Controller.AuthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Controller.UserProfileActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>